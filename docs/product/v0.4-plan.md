# BranchDojo v0.4 Plan

## Status

Planning baseline. No v0.4 features are implemented yet.

## Objective

Expand BranchDojo from beginner and intermediate recovery workflows into advanced Git practice while preserving safe disposable repositories and final-state validation.

## Product Theme

Advanced Git practice and reusable exercise foundations.

## Target Users

- Developers who have completed the beginner and intermediate exercises.
- Developers practicing history cleanup, release repair, and diagnostic workflows.
- Mentors and onboarding programs that want a clearer learning path.
- Teams that need local practice artifacts without dashboards or remote tracking.

## Problems To Solve

- The current catalog stops before advanced workflows such as rebase cleanup, bisection, release tags, and merge strategy comparison.
- Exercise ordering is visible in docs but not expressed as a clear learning path.
- The metadata model may need an `advanced` difficulty before advanced exercises ship.
- Future exercise packs and custom definitions need research, but not implementation, before the design can stay safe.

## Proposed Scope

- Plan 2 or 3 advanced built-in exercises for v0.4.
- Add `advanced` difficulty support when the first advanced exercise is implemented.
- Document beginner, intermediate, and advanced learning paths.
- Keep external exercise-pack work as research only.
- Preserve current CLI behavior unless a later v0.4 task explicitly approves a small docs-backed addition.

## Explicit Non-Goals

- No GUI or TUI.
- No GitHub integration.
- No remote repository workflows.
- No telemetry or analytics.
- No classroom dashboard.
- No external exercise pack loading in this planning pass.
- No custom exercise parser.
- No YAML, TOML, or JSON exercise definitions.
- No crates.io publishing unless separately planned.
- No shell-history or command-history tracking.
- No global Git config changes.
- No unsafe repository mutation outside BranchDojo workspaces.
- No report templates.
- No PDF or HTML reports.

## Candidate Exercise 1: interactive-rebase-basic

### Purpose

Practice cleaning up local history by squashing or rewording commits before sharing work.

### Starting State

A disposable repository contains `main` and a feature branch with several small commits, including a typo-fix commit and a noisy commit message.

### User Task

Rewrite the feature branch into a clean final history while preserving the intended file content.

### Expected Final State

The user is on the expected feature branch, the working tree is clean, the final file content is correct, and the branch history has the intended cleaned-up shape.

### Required Checks

- Valid BranchDojo metadata exists.
- Repository is on the expected feature branch.
- Working tree is clean.
- No active Git operation remains.
- Expected final content exists.
- Unwanted intermediate content is absent.
- Required commits or commit subjects are present in the cleaned history.

### Warning Conditions

- Final content is correct but history shape suggests a normal follow-up commit instead of a cleaned-up branch.
- Commit messages are acceptable but not ideal for the intended workflow.

### Risks

Interactive rebase is command-sequence-heavy. Validation must stay focused on final repository state and observable history shape.

### Test Strategy

Use integration tests with deterministic commit graphs. Include a passing cleaned-history solution and a warning case where the final content is correct but history remains noisy.

## Candidate Exercise 2: bisect-basic

### Purpose

Practice finding the commit that introduced a regression using a deterministic local failure signal.

### Starting State

A repository contains a linear history where one commit changes a small file from a passing state to a failing state.

### User Task

Identify the bad commit and record the result in a required file or branch state without relying on external test infrastructure.

### Expected Final State

The repository records the expected bad commit identifier or subject in a local file, returns to the expected branch, and has a clean working tree.

### Required Checks

- Valid BranchDojo metadata exists.
- Repository is on the expected branch.
- Working tree is clean.
- No active Git operation remains.
- The recorded bad commit matches the seeded regression commit.
- Expected files remain intact.

### Warning Conditions

- The bad commit is correctly identified but the repository is left on a detached HEAD or unexpected branch.
- The final state is correct but includes extra diagnostic files.

### Risks

`git bisect run` often uses scripts. BranchDojo should avoid requiring shell scripts and keep any failure signal inspectable through repository state.

### Test Strategy

Keep the regression signal file-based and deterministic. Add tests for correct identification, wrong commit identification, detached HEAD cleanup, and no-regression behavior for existing exercises.

## Candidate Exercise 3: merge-vs-rebase

### Purpose

Practice choosing between preserving branch topology with a merge and creating a linear history with a rebase.

### Starting State

A repository contains `main` and a feature branch that both moved forward from a shared base.

### User Task

Integrate the feature work according to the exercise instructions while preserving the required content.

### Expected Final State

The expected branch contains both mainline and feature changes, the working tree is clean, and the history shape matches the selected workflow or receives a warning when content is valid but shape is not ideal.

### Required Checks

- Valid BranchDojo metadata exists.
- Repository is on the expected branch.
- Working tree is clean.
- No active Git operation remains.
- Required content from both branches exists.
- Forbidden conflict markers are absent.

### Warning Conditions

- Final content is correct but the history shape uses the non-preferred integration style.
- Extra merge or fixup commits are present but do not break final content.

### Risks

The exercise may be clearer as two separate exercises if a single scenario tries to teach both merge and rebase tradeoffs.

### Test Strategy

Test both valid content and preferred history shape. Include a warning case for the alternate integration style.

## Candidate Exercise 4: tag-release-fix

### Purpose

Practice repairing a release branch and placing a release tag on the corrected commit.

### Starting State

A repository contains a release branch, an incorrect release tag, and a later fix commit or fix branch.

### User Task

Move the release state to the corrected commit and create the expected release tag shape.

### Expected Final State

The release branch points to the corrected content, the expected tag exists on the corrected commit, and the working tree is clean.

### Required Checks

- Valid BranchDojo metadata exists.
- Repository is on the expected release branch.
- Working tree is clean.
- No active Git operation remains.
- Corrected release content exists.
- Expected tag exists and points to the corrected commit.
- Incorrect release content is absent.

### Warning Conditions

- A lightweight tag is used when the exercise prefers an annotated tag.
- The corrected content is present but extra release repair commits make history less clear.

### Risks

Tag validation must be precise without requiring remote operations or tag force-push behavior.

### Test Strategy

Test corrected annotated tag, lightweight-tag warning, wrong-tag-target failure, and no-regression behavior for existing reports and JSON output.

## Learning Path Plan

- Beginner path: `conflict-basic`, `revert-mistake`, `wrong-branch-commit`.
- Intermediate path: `stash-switch`, `cherry-pick-basic`, `detached-head-recovery`.
- Advanced path: selected v0.4 exercises after implementation.

Learning paths should be documentation-only in the planning baseline.

## Exercise Metadata Impact

v0.4 may need:

- `advanced` difficulty.
- Optional learning-path grouping.
- Stable category names for advanced workflows.

Metadata changes should be minimal and tested for catalog drift.

## CLI Impact

No CLI changes are planned in the baseline. Possible future additions, such as filtering `branchdojo list` by difficulty or category, require separate design and tests.

## Validation Impact

Advanced exercises should continue to validate final repository state and observable history shape, not exact command sequence.

Validators may need new exercise-specific checks for cleaned history, tag target, bad commit identification, or integration style.

## Safety Impact

v0.4 must preserve the existing safety model:

- Work only inside BranchDojo-created disposable repositories.
- Refuse unsafe paths.
- Refuse non-BranchDojo workspaces for check, hint, reset, and reports.
- Use explicit Git command arguments.
- Do not modify global Git config.
- Do not require shell scripts.

## Testing Strategy

- Unit-test metadata additions.
- Integration-test setup for each selected advanced exercise.
- Integration-test failing starting state, passing solved state, and warning shape.
- Preserve report and JSON output compatibility.
- Preserve progress-aware hints for all released exercises.
- Keep no-regression tests for all six released exercises.

## Implementation Order

1. Add `advanced` difficulty support if needed.
2. Implement `interactive-rebase-basic`.
3. Implement `bisect-basic` only if deterministic validation remains simple.
4. Implement `tag-release-fix` or `merge-vs-rebase`, not both unless scope stays small.
5. Update learning path docs.
6. Release polish.

## Release Criteria

- v0.4 scope is accurately reflected in README, roadmap, and changelog.
- Selected advanced exercises are implemented and documented.
- All six released exercises retain current behavior.
- New validators preserve final-state validation.
- Hints and reports work for new exercises.
- `cargo fmt --check` passes.
- `cargo clippy --all-targets --all-features -- -D warnings` passes.
- `cargo test` passes.
- CI passes on `main`.

## Open Decisions

- Which advanced exercises should be implemented in v0.4?
- Should `advanced` difficulty be added now or only when the first advanced exercise is implemented?
- Should `branchdojo list` later support filtering by difficulty or category?
- Should learning paths stay docs-only in v0.4?
- Should external exercise packs be deferred to v0.5 or later?
- Should custom exercise definitions use Rust-only modules for now?
- Should `bisect-basic` be included, given it requires deterministic test behavior?
- Should `interactive-rebase-basic` be validated by final-state and history shape only?
- Should `merge-vs-rebase` be a single exercise or two separate exercises?
- Should `tag-release-fix` require annotated tags or accept lightweight tags with a warning?

## Risks

- Advanced workflows can push the product toward command-sequence validation.
- Bisect scenarios can drift into script execution if not carefully scoped.
- External exercise-pack research could overtake built-in exercise quality.
- Learning-path metadata could become premature CLI surface area.

## Deferred Items

- JSON report files.
- HTML and PDF reports.
- Report templates.
- Dashboards.
- `--force` report overwrite behavior.
- External exercise packs.
- Custom exercise definitions.
- TUI or GUI.
- GitHub integration.
- Release automation.
